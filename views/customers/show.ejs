<%- include('../partials/header') %>
<p><%= user._id %></p>

<% if (business.customers.length) { %>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Address</th>
        </tr>
      </thead>
      <tbody>
        <% business.customers.forEach(function(c) { %>
          <tr>
            <td><%= c.name %></td>
            <td><%= c.address %></td>
            <td>
            <form action="/businesses/<%= business._id %>/customers/<%= c._id %>?_method=DELETE"
                class="delete-form" method="POST">
                <button type="submit">X</button>
              </form>
            </td>
            <td>
                <a href="/businesses/<%= business._id %>/customers/<%= c._id %>/edit">Edit Customer</a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <h5>No Customers Added</h5>
  <% } %>


<%- include('../partials/footer') %>